--- a/purpur-server/build.gradle.kts
+++ b/purpur-server/build.gradle.kts
@@ -33,9 +_,33 @@
             outputDir = rootDirectory.dir("paper-server")
         }
     }
-    activeFork = purpur
+    //activeFork = purpur
     // Purpur end - Rebrand
 
+    // Neil start - Rebrand
+    val neil = forks.register("neil") {
+        forks = purpur
+        upstream.patchRepo("paperServer") {
+            upstreamRepo = purpur.patchedRepo("paperServer")
+            patchesDir = rootDirectory.dir("neil-server/paper-patches")
+            outputDir = rootDirectory.dir("paper-server")
+        }
+        upstream.patchDir("purpurServer") {
+            upstreamPath = "purpur-server"
+            excludes = setOf(
+                "src/minecraft",
+                "paper-patches",
+                "minecraft-patches",
+                "build.gradle.kts",
+                "build.gradle.kts.patch"
+            )
+            patchesDir = rootDirectory.dir("neil-server/purpur-patches")
+            outputDir = rootDirectory.dir("purpur-server")
+        }
+    }
+    activeFork = neil
+    // Neil end - Rebrand
+
     spigot {
         enabled = true
         buildDataRef = "42d18d4c4653ffc549778dbe223f6994a031d69e"
@@ -122,10 +_,14 @@
     main {
         java { srcDir("../paper-server/src/main/java") }
         resources { srcDir("../paper-server/src/main/resources") }
+        java { srcDir("../purpur-server/src/main/java") }
+        resources { srcDir("../purpur-server/src/main/resources") }
     }
     test {
         java { srcDir("../paper-server/src/test/java") }
         resources { srcDir("../paper-server/src/test/resources") }
+        java { srcDir("../purpur-server/src/test/java") }
+        resources { srcDir("../purpur-server/src/test/resources") }
     }
 }
 val log4jPlugins = sourceSets.create("log4jPlugins") {
@@ -154,8 +_,8 @@
 }
 
 dependencies {
-    implementation(project(":purpur-api")) // Purpur
-    implementation("ca.spottedleaf:concurrentutil:0.0.7")
+    implementation(project(":neil-api")) // Purpur // Neil
+    implementation("ca.spottedleaf:concurrentutil:0.0.5")
     implementation("org.jline:jline-terminal-ffm:3.27.1") // use ffm on java 22+
     implementation("org.jline:jline-terminal-jni:3.27.1") // fall back to jni on java 21
     implementation("net.minecrell:terminalconsoleappender:1.3.0")
@@ -228,14 +_,14 @@
         val gitBranch = git.exec(providers, "rev-parse", "--abbrev-ref", "HEAD").get().trim()
         attributes(
             "Main-Class" to "org.bukkit.craftbukkit.Main",
-            "Implementation-Title" to "Purpur", // Purpur
+            "Implementation-Title" to "Neil", // Purpur // Neil
             "Implementation-Version" to implementationVersion,
             "Implementation-Vendor" to date,
-            "Specification-Title" to "Purpur", // Purpur
+            "Specification-Title" to "Neil", // Purpur // Neil
             "Specification-Version" to project.version,
-            "Specification-Vendor" to "Purpur Team", // Purpur
-            "Brand-Id" to "purpurmc:purpur", // Purpur
-            "Brand-Name" to "Purpur", // Purpur
+            "Specification-Vendor" to "En-V Arcade", // Purpur // Neil
+            "Brand-Id" to "envarcade:neil", // Purpur // Neil
+            "Brand-Name" to "Neil", // Purpur // Neil
             "Build-Number" to (build ?: ""),
             "Build-Time" to buildTime.toString(),
             "Git-Branch" to gitBranch,
@@ -386,22 +_,38 @@
     mainClass.set(null as String?)
 }
 
-/* fill { // Purpur - we don't use fill
-    project("paper")
-    versionFamily(paperweight.minecraftVersion.map { it.split(".", "-").takeWhile { part -> part.toIntOrNull() != null }.take(2).joinToString(".") })
-    version(paperweight.minecraftVersion)
+fill {
+    // Where to send requests
+    apiUrl("https://api.envarcade.dev")
+
+    apiToken = providers.gradleProperty("fillApiToken").orNull
+
+    // Project & version identity on Fill
+    project("neil")
+    versionFamily("1.21")
+    version("1.21.10")
 
     build {
+        // Jenkins build number as the Fill build id
+        val buildNumber = System.getenv("BUILD_NUMBER")?.toIntOrNull() ?: 1
+        id = buildNumber
+
         channel = BuildChannel.STABLE
 
         downloads {
-            register("server:default") {
-                file = tasks.createMojmapPaperclipJar.flatMap { it.outputZip }
-                nameResolver.set { project, _, version, build -> "$project-$version-$build.jar" }
+            // Name of this downloadable artifact
+            register("server") {
+                // The file to upload: the server jar produced by this module
+                file = tasks.jar.get().archiveFile
+
+                // How it will be named on Fill
+                nameResolver.set { project, _, version, build ->
+                    "$project-$version-$build.jar"
+                }
             }
         }
     }
-} */ // Purpur - we don't use fill
+}
 
 // tasks.register("rebuildMinecraftSourcesWithGit") {
 //     group = "temp"
